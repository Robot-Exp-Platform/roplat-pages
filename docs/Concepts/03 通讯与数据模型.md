# 通讯与数据模型

## 主流通讯与旁路通讯

1. 主流通讯：节点输入输出主链，强类型、可分析。
2. 旁路通讯：用于状态同步等扩展场景。

## 透明类型与不透明类型

1. 透明类型：跨语言可见布局。
2. 不透明类型：Rust 侧只保留类型标记，字段由目标语言用户文件定义。

## 为什么 opaque 要“空壳化”

核心是避免双重真相：

1. 如果 Rust 和 C++ 同时定义字段，长期容易漂移。
2. 空壳化后，Rust 负责调度，目标语言负责数据实现。

## 工程落地规则

1. 能用透明类型就先用透明类型。
2. 确认是语言私有实现再引入 opaque。
3. 每次引入新数据类型后都跑一遍 lint 与构建验证。
